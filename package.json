{
  "devDependencies": {
    "tailwindcss": "^3.4.3",
    "vite": "^5.2.9"
  },
  "dependencies": {
    "three": "^0.163.0"
  },
  "scripts": {
    "build": "vite build && npx tailwindcss -i .dist/src/style.css -o output.css --minify"
  }
}
